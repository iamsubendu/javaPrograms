/**
 * Write a program in java to check whether the given
 * number is an even number or not using ternary Operator.
 */
public class Fourth {
    public static void main(String[] args) {
        int Number = 10;
        String result = (Number % 2 == 0 ? "Even" : "Odd");
        System.out.println(result);
    }
}
